import 'package:flutter/material.dart';

class HistoryPermitPage extends StatelessWidget {
  const HistoryPermitPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text(
        'Fitur Belum Tersedia',
        style: TextStyle(fontFamily: 'ROBOTO', fontSize: 32, color: Colors.red),
      )),
    );
  }
}
