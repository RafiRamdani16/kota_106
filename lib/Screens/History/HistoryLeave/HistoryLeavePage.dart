import 'package:flutter/material.dart';

class HistoryLeavePage extends StatelessWidget {
  const HistoryLeavePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text(
        'Fitur Belum Tersedia',
        style: TextStyle(fontFamily: 'ROBOTO', fontSize: 32, color: Colors.red),
      )),
    );
  }
}
